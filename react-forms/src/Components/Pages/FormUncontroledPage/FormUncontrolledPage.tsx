import { FC } from 'react';

const FormUncontrolledPage: FC = () => {
  return (
    <section className="error-page">
      <h2 className="page-header">Uncontrolled Form to be here</h2>
    </section>
  );
};

export default FormUncontrolledPage;
